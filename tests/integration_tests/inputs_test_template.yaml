---

# hail setup parameter
tmp_dir: file://{INTEGRATION_TESTS_DIR}/tmp

# --- Step 1.1 --- #
# gatk_vcf_header: file://{INTEGRATION_TESTS_DIR}/gatk_vcf_header.txt # TODO: make optional
gatk_import_lustre_dir: file://{TEST_DATA_DIR} # test with the small dataset
load_matrixtables_lustre_dir: {INTEGRATION_TESTS_DIR}/matrixtables/ # not used in this step
# matrixtables_lustre_dir: file://{INTEGRATION_TESTS_DIR}/matrixtables/ # used in this step 

# --- Step 2.1 --- #
#load_matrixtables_lustre_dir: /lustre/scratch126/dh24_test/qc_test/matrixtables/mts_for_control_datasets/
annotation_lustre_dir: file://{INTEGRATION_TESTS_DIR}/annotations/
annotation_lustre_dir_local: {INTEGRATION_TESTS_DIR}/annotations/ # TODO: clarify the purpose of these _local duplicates, not used in this step
matrixtables_lustre_dir: file://{INTEGRATION_TESTS_DIR}/matrixtables/
resource_dir: file://{RESOURCES_DIR}

# --- Step 2.2 --- #
#matrixtables_lustre_dir: file:///lustre/scratch126/dh24_test/qc_test/matrixtables/mts_for_control_datasets/
plots_lustre_dir: file://{INTEGRATION_TESTS_DIR}/plots/ # TODO: not used?
plots_dir_local: {INTEGRATION_TESTS_DIR}/plots/
# resource_dir: file://{INTEGRATION_TESTS_DIR}/resources/
resource_dir_local: {INTEGRATION_TESTS_DIR}/resources/
training_set_dir: file://{INTEGRATION_TESTS_DIR}/training_sets/
var_qc_rf_dir: file://{INTEGRATION_TESTS_DIR}/variant_qc_random_forest/
vcf_output_dir: file://{INTEGRATION_TESTS_DIR}/filtered_vcfs/

rf_test_interval: chr20

#hard filter
hard_filters:
  missingness: 0.5
  snp:
    relaxed:
      bin: 92
      dp: 5
      gq: 10
      ab: 0.2
    medium:
      bin: 88
      dp: 5
      gq: 10
      ab: 0.2
    stringent:
      bin: 84
      dp: 5
      gq: 15
      ab: 0.2
  indel:
    relaxed:
      bin: 68
      dp: 5
      gq: 10
      ab: 0.2
    medium:
      bin: 66
      dp: 5
      gq: 15
      ab: 0.2
    stringent:
      bin: 58
      dp: 5
      gq: 20
      ab: 0.3
